[project]
name = "alert-app"
version = "0.1.0"
description = "Streamlit dashboard for transaction anomaly alerts"
requires-python = ">=3.9"
dependencies = [
    "streamlit==1.40.1",
    "confluent-kafka==2.6.1",
    "pandas==2.2.3",
    "pymongo==4.10.1",
]

[tool.uv]
package = false

[project]
name = "simulator"
version = "0.1.0"
description = "Transaction stream simulator"
requires-python = ">=3.9"
dependencies = [
    "confluent-kafka==2.6.1",
    "faker==30.8.2",
]

[tool.uv]
package = false
